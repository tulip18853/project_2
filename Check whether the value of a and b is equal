#include<stdio.h>
#include<conio.h>
void main(){
    int a,b;
    printf("enter the two values");
    scanf("%d%d",&a,&b);
    if(a==b)
    {
        printf("%d is equl to %d",a,b);
    }
    getch();
}
